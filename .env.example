# Required for production (Supabase)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: Raider.io API key (higher rate limits)
# RAIDERIO_API_KEY=

# Local dev uses SQLite by default (no DATABASE_URL needed)
